/**
 * Temporarily pauses every public studio-rental surface while preserving the
 * booking page, form, services, styles, and mail infrastructure for later.
 *
 * When rentals resume, set this to true and remove the two /book-studio rules
 * from public/_redirects.
 */
export const studioRentalsEnabled = false;
